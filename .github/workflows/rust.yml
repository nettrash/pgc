name: Rust

on:
  push:
    branches: [ "*" ]
  pull_request:
    branches: [ "*" ]
  workflow_dispatch:  # Enable manual triggering

env:
  CARGO_TERM_COLOR: always

jobs:
  build:
    runs-on: ubuntu-latest

    # Matrix validates the MSRV-ish floor (1.88.0) AND the toolchain used by
    # the Dockerfile (1.95). Keep the upper bound in sync with `Dockerfile`.
    strategy:
      fail-fast: false
      matrix:
        rust: ["1.88.0", "1.95.0"]

    steps:
    - uses: actions/checkout@v4

    - name: Install Rust
      uses: dtolnay/rust-toolchain@stable
      with:
        toolchain: ${{ matrix.rust }}
        components: rustfmt, clippy

    - name: Check format
      run:
        cd ${{ github.workspace }}/app ;
          cargo fmt -- --check
    - name: Check clippy
      run:
        cd ${{ github.workspace }}/app ;
          cargo clippy --all-targets --all-features
    - name: Build
      run:
        cd ${{ github.workspace }}/app ;
        cargo build --verbose
    # Documents private items too: pgc ships as a binary, so most of the code
    # a contributor reads (comparer::core is 4 pub fns out of 47) is private,
    # and a broken intra-doc link there is just as wrong. -D warnings turns
    # unresolved links into a build failure instead of silent plain text.
    - name: Check docs
      env:
        RUSTDOCFLAGS: "-D warnings"
      run:
        cd ${{ github.workspace }}/app ;
        cargo doc --no-deps --lib --document-private-items
    - name: Run tests
      run:
        cd ${{ github.workspace }}/app ;
        cargo test --verbose

  integration:
    needs: build
    runs-on: ubuntu-latest

    # Round-trip test against every supported PostgreSQL major version.
    # For each PG version the job:
    #   1. Spins up a fresh postgres container (with wal_level=logical so
    #      the publication tests in the schemas don't fail at CREATE).
    #   2. Creates two databases `a` and `b`.
    #   3. Applies `data/test/schema_a.sql` to `a` and `schema_b.sql` to `b`.
    #      Uses ON_ERROR_STOP=0 because some statements use features specific
    #      to newer PG versions (NULLS NOT DISTINCT on PG15+, NOT ENFORCED on
    #      PG18+); they fail to apply on older versions but the round-trip
    #      property still holds because pgc emits SQL based on dumps, not on
    #      the source schema files.
    #   4. Runs pgc compare to produce `output.sql`.
    #   5. Applies `output.sql` to `a`.
    #   6. Re-runs pgc compare. The new diff must be empty — that's the
    #      idempotency contract pgc must satisfy.
    strategy:
      fail-fast: false
      matrix:
        pg_version: ["14", "15", "16", "17", "18"]

    steps:
    - uses: actions/checkout@v4

    - name: Install Rust
      uses: dtolnay/rust-toolchain@stable
      with:
        toolchain: "1.95.0"

    - name: Install PostgreSQL client
      run: |
        sudo apt-get update
        sudo apt-get install -y postgresql-client

    - name: Start PostgreSQL ${{ matrix.pg_version }}
      run: |
        # `services:` does not let us pass `command:` to the container, and
        # `wal_level=logical` (required by the publication test cases) needs
        # to be set at server start, so we run docker manually.
        docker run -d \
          --name pg \
          -e POSTGRES_PASSWORD=postgres \
          -e POSTGRES_USER=postgres \
          -p 5432:5432 \
          postgres:${{ matrix.pg_version }} \
          -c wal_level=logical
        # Wait for the server to accept connections.
        for i in $(seq 1 60); do
          if docker exec pg pg_isready -U postgres -h 127.0.0.1 >/dev/null 2>&1; then
            echo "PostgreSQL ${{ matrix.pg_version }} ready after ${i}s"
            exit 0
          fi
          sleep 1
        done
        echo "PostgreSQL did not become ready in time"
        docker logs pg
        exit 1

    - name: Create test databases
      env:
        PGPASSWORD: postgres
      run: |
        psql -h 127.0.0.1 -U postgres -c "CREATE DATABASE a;"
        psql -h 127.0.0.1 -U postgres -c "CREATE DATABASE b;"

    - name: Apply schemas
      env:
        PGPASSWORD: postgres
      run: |
        psql -h 127.0.0.1 -U postgres -d a -v ON_ERROR_STOP=0 \
          -f data/test/schema_a.sql
        psql -h 127.0.0.1 -U postgres -d b -v ON_ERROR_STOP=0 \
          -f data/test/schema_b.sql

    - name: Build pgc (release)
      run: |
        cd ${{ github.workspace }}/app
        cargo build --release

    - name: Generate pgc config
      run: |
        cat > /tmp/pgc.conf <<'EOF'
        FROM_HOST=127.0.0.1
        FROM_PORT=5432
        FROM_USER=postgres
        FROM_PASSWORD=postgres
        FROM_DATABASE=a
        FROM_SCHEME=test_schema|shared_schema|new_reporting_schema|data|test_order|public
        FROM_SSL=false
        FROM_DUMP=/tmp/a.dump
        TO_HOST=127.0.0.1
        TO_PORT=5432
        TO_USER=postgres
        TO_PASSWORD=postgres
        TO_DATABASE=b
        TO_SCHEME=test_schema|shared_schema|new_reporting_schema|data|test_order|public
        TO_SSL=false
        TO_DUMP=/tmp/b.dump
        OUTPUT=/tmp/output.sql
        USE_DROP=true
        USE_COMMENTS=false
        GRANTS_MODE=full
        MAX_CONNECTIONS=10
        USE_SINGLE_TRANSACTION=false
        EOF

    - name: Run pgc compare (round 1) — generate diff
      run: |
        ./app/target/release/pgc --config /tmp/pgc.conf
        echo "--- output.sql (round 1) size: $(wc -c < /tmp/output.sql) bytes"

    - name: Apply diff to database `a`
      env:
        PGPASSWORD: postgres
      run: |
        # ON_ERROR_STOP=0 mirrors the schema-apply step above: a diff that
        # references PG-version-specific syntax may not apply on every
        # version, but as long as the round-trip closes (round-2 diff is
        # empty) the test still validates idempotency.
        psql -h 127.0.0.1 -U postgres -d a -v ON_ERROR_STOP=0 \
          -f /tmp/output.sql

    - name: Assert unread-column changes did not drag views along
      run: |
        # A materialized view is dropped and fully rebuilt whenever the diff
        # decides it depends on a changing table. Deciding that at table
        # granularity turned a metadata-only ADD COLUMN into a full refresh
        # (issue #242). The round-2 diff cannot catch it — an unnecessary
        # drop+recreate still converges, so the compare is empty either way —
        # so assert directly against the round-1 script.
        #
        # The fixtures: test_schema.i242_orders gains a column and retypes
        # `status`. i242_mv_untouched and i242_v_untouched read only `id`, so
        # PostgreSQL permits both changes with them in place and neither may
        # be dropped; i242_mv_touched reads `status` and must still go.
        fail=0
        for v in i242_mv_untouched i242_v_untouched; do
          if grep -qE "drop (materialized )?view if exists test_schema.${v}" /tmp/output.sql; then
            echo "ERROR: ${v} was dropped although it reads no changed column (PG ${{ matrix.pg_version }})"
            fail=1
          fi
        done
        if grep -q "ix_i242_mv_untouched_id" /tmp/output.sql; then
          echo "ERROR: the index of an untouched materialized view was rebuilt (PG ${{ matrix.pg_version }})"
          fail=1
        fi
        if ! grep -q "drop materialized view if exists test_schema.i242_mv_touched" /tmp/output.sql; then
          echo "ERROR: i242_mv_touched reads a retyped column and must be dropped first (PG ${{ matrix.pg_version }})"
          fail=1
        fi
        [ "${fail}" -eq 0 ] || exit 1
        echo "OK: only the views reading changed columns were dropped on PG ${{ matrix.pg_version }}"

    - name: Assert all indexes are valid
      env:
        PGPASSWORD: postgres
      run: |
        # An index on a partitioned parent emitted as `CREATE INDEX ... ON ONLY`
        # without the per-partition attach sequence stays indisvalid=false
        # (issue #223). The round-2 diff cannot catch it — the invalid index
        # still exists, so the compare is empty — so assert directly that no
        # index the migration produced was left invalid.
        invalid=$(psql -h 127.0.0.1 -U postgres -d a -tAc \
          "select string_agg(format('%I.%I', n.nspname, c.relname), ', ')
           from pg_index x
           join pg_class c on c.oid = x.indexrelid
           join pg_namespace n on n.oid = c.relnamespace
           where not x.indisvalid
             and n.nspname not in ('pg_catalog', 'information_schema');")
        if [ -n "${invalid}" ]; then
          echo "ERROR: invalid indexes after applying the migration on PG ${{ matrix.pg_version }}: ${invalid}"
          exit 1
        fi
        echo "OK: all indexes valid on PG ${{ matrix.pg_version }}"

    - name: Run pgc compare (round 2) — must be empty
      run: |
        ./app/target/release/pgc --config /tmp/pgc.conf
        size=$(wc -c < /tmp/output.sql)
        echo "--- output.sql (round 2) size: ${size} bytes"
        # With USE_COMMENTS=false a fully-idempotent diff produces a 0-byte
        # file; a non-empty file (even one containing only whitespace) means
        # the comparer emitted SQL that wasn't covered by round 1.
        if [ "${size}" -gt 0 ] && [ -n "$(tr -d '[:space:]' < /tmp/output.sql)" ]; then
          echo "ERROR: round-2 diff is not empty on PG ${{ matrix.pg_version }}"
          echo "--- output.sql contents: ---"
          cat /tmp/output.sql
          exit 1
        fi
        echo "OK: pgc is idempotent on PG ${{ matrix.pg_version }}"

    - name: Stop PostgreSQL
      if: always()
      run: |
        docker logs pg | tail -200 || true
        docker stop pg || true
        docker rm pg || true

  docker:
    needs: [build, integration]
    runs-on: ubuntu-latest
    # Only run automatically on master branch, or when manually triggered
    if: github.ref == 'refs/heads/master' || github.event_name == 'workflow_dispatch'
    
    steps:
    - uses: actions/checkout@v4

    - name: Read crate version
      id: crate
      run: |
        version=$(grep -m1 '^version' app/Cargo.toml | cut -d '"' -f2)
        echo "version=${version}" >> "$GITHUB_OUTPUT"

    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v3
    
    - name: Log in to Docker Hub
      if: github.event_name != 'pull_request'
      uses: docker/login-action@v3
      with:
        username: ${{ secrets.DOCKER_USERNAME }}
        password: ${{ secrets.DOCKER_PASSWORD }}
      continue-on-error: true
    
    - name: Extract metadata
      id: meta
      uses: docker/metadata-action@v5
      with:
        images: nettrash/pgc
        tags: |
          type=ref,event=branch
          type=ref,event=pr
          type=sha,prefix={{branch}}-
          type=raw,value=latest,enable={{is_default_branch}}
    
    - name: Build Docker image for testing (single platform)
      uses: docker/build-push-action@v5
      with:
        context: .
        platforms: linux/amd64
        push: false
        tags: pgc:test
        labels: ${{ steps.meta.outputs.labels }}
        build-args: |
          PGC_VERSION=${{ steps.crate.outputs.version }}
        cache-from: type=gha
        cache-to: type=gha,mode=max
        load: true
    
    - name: Test Docker image locally
      run: |
        echo "Testing image: pgc:test"
        docker run --rm pgc:test pgc --help
    
    - name: Build and push Docker image (multi-platform)
      # Push to registry if: not a PR AND (on master branch OR manually triggered)
      if: github.event_name != 'pull_request' && (github.ref == 'refs/heads/master' || github.event_name == 'workflow_dispatch')
      uses: docker/build-push-action@v5
      with:
        context: .
        platforms: linux/amd64,linux/arm64
        push: true
        tags: ${{ steps.meta.outputs.tags }}
        labels: ${{ steps.meta.outputs.labels }}
        build-args: |
          PGC_VERSION=${{ steps.crate.outputs.version }}
        cache-from: type=gha
        cache-to: type=gha,mode=max
      continue-on-error: true
